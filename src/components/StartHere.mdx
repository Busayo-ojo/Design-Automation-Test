import { Meta } from '@storybook/blocks';

<Meta title="Components/🚀 Start Here" />

<style>
{`
  .component-card-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
    margin-top: 32px;
    margin-bottom: 48px;
  }
  .component-card {
    border: 1px solid #e4e7ec;
    border-radius: 12px;
    padding: 24px;
    background: #ffffff;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.02);
    transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
  }
  .component-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 16px rgba(29, 50, 109, 0.06);
    border-color: #1d326d;
  }
  .component-card h3 {
    margin-top: 0;
    margin-bottom: 8px;
    color: #1d326d;
    font-size: 18px;
    font-weight: 700;
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .component-card p {
    margin: 0;
    color: #475367;
    font-size: 14px;
    line-height: 1.5;
  }
  .intro-box {
    background: #183972;
    color: white;
    padding: 40px;
    border-radius: 16px;
    margin-bottom: 40px;
    box-shadow: 0 8px 32px rgba(24, 57, 114, 0.15);
  }
  .intro-box h1 {
    color: white !important;
    margin-top: 0;
    margin-bottom: 16px;
    font-size: 32px;
    font-weight: 800;
  }
  .intro-box p {
    color: #e3effc;
    font-size: 18px;
    font-weight: 300;
    line-height: 1.8;
    margin: 0;
  }
`}
</style>

<div className="intro-box">
  <h1>Design Components</h1>
  <p>
    Components are higher-level UI modules (molecules/organisms) that combine foundations and atomic elements to solve user workflow needs. They manage layout, local states, and interaction behaviors.
  </p>
</div>

## Available Components

Explore our components designed to provide unified digital solutions:

<div className="component-card-grid">
  <div className="component-card">
    <h3>✨ Card & Table</h3>
    <p>
      Content containers. Card groups related topics. Table presents dense structured tabular datasets, supporting headers and records.
    </p>
  </div>
  <div className="component-card">
    <h3>✨ Dropdown & Textarea</h3>
    <p>
      Advanced inputs. Dropdown provides list selections, and Textarea allows multiline text input.
    </p>
  </div>
  <div className="component-card">
    <h3>✨ Modal & Drawer</h3>
    <p>
      Overlays used to focus user attention on a task, display detailed data, or present supplementary workflows without leaving the primary context.
    </p>
  </div>
  <div className="component-card">
    <h3>✨ NavMenu & Tabs</h3>
    <p>
      Navigation patterns. NavMenu directs site structures, while Tabs switch internal views in a single screen workspace.
    </p>
  </div>
</div>

## Design Philosophy

Components should be flexible, responsive, and composable. Use slots, sub-components, and render props to support custom layouts without breaking the design guidelines.
