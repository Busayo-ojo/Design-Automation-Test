import { Meta } from '@storybook/addon-docs/blocks';
import { ChangelogComponent } from './ChangelogComponent';

<Meta title="🪵 Changelog" />

# Changelog

All notable changes to the `@fmdq/ui` library are dynamically loaded directly from the repository's git commit log below. Use the search bar and filters to browse specific releases and design token updates.

<br />

<ChangelogComponent />
